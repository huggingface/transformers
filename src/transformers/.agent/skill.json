{
  "name": "transformers",
  "capabilities": [
    {
      "id": "classify",
      "description": "Classify text into categories.",
      "inputs": {
        "text": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Text to classify."
        },
        "file": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Read text from this file."
        },
        "labels": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Comma-separated candidate labels for zero-shot classification."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "labels": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "scores": {
            "type": "array",
            "items": {
              "type": "number"
            }
          }
        }
      },
      "invocation": {
        "cli": "transformers classify --text <text> --file <file> --labels <labels> --model <model> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "ner",
      "description": "Extract named entities from text (NER).",
      "inputs": {
        "text": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Text to extract entities from."
        },
        "file": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Read text from this file."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        },
        "aggregation_strategy": {
          "type": "string",
          "default": "simple",
          "description": "Entity aggregation: 'none' or 'simple'."
        }
      },
      "outputs": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "entity_group": {
              "type": "string"
            },
            "score": {
              "type": "number"
            },
            "word": {
              "type": "string"
            },
            "start": {
              "type": "integer"
            },
            "end": {
              "type": "integer"
            }
          }
        }
      },
      "invocation": {
        "cli": "transformers ner --text <text> --file <file> --model <model> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision> --aggregation-strategy <aggregation_strategy>"
      }
    },
    {
      "id": "token-classify",
      "description": "Tag tokens with labels (POS tagging, chunking, etc.).",
      "inputs": {
        "text": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Text to tag."
        },
        "file": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Read text from this file."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "entity": {
              "type": "string"
            },
            "score": {
              "type": "number"
            },
            "word": {
              "type": "string"
            },
            "start": {
              "type": "integer"
            },
            "end": {
              "type": "integer"
            }
          }
        }
      },
      "invocation": {
        "cli": "transformers token-classify --text <text> --file <file> --model <model> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "qa",
      "description": "Answer a question given a context paragraph (extractive QA).",
      "inputs": {
        "question": {
          "type": "string",
          "description": "The question to answer."
        },
        "context": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Context paragraph containing the answer."
        },
        "file": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Read context from this file."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "answer": {
            "type": "string"
          },
          "score": {
            "type": "number"
          },
          "start": {
            "type": "integer"
          },
          "end": {
            "type": "integer"
          }
        }
      },
      "invocation": {
        "cli": "transformers qa <question> --context <context> --file <file> --model <model> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "table-qa",
      "description": "Answer a question about tabular data (CSV).",
      "inputs": {
        "question": {
          "type": "string",
          "description": "Question about the table."
        },
        "table": {
          "type": "string",
          "description": "Path to a CSV file containing the table."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "answer": {
            "type": "string"
          },
          "coordinates": {
            "type": "array"
          },
          "cells": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "aggregator": {
            "type": "string"
          }
        }
      },
      "invocation": {
        "cli": "transformers table-qa <question> <table> --model <model> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "summarize",
      "description": "Summarize text.",
      "inputs": {
        "text": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Text to summarize."
        },
        "file": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Read text from this file."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "max_length": {
          "type": "integer",
          "nullable": true,
          "default": null,
          "description": "Maximum summary length in tokens."
        },
        "min_length": {
          "type": "integer",
          "nullable": true,
          "default": null,
          "description": "Minimum summary length in tokens."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "summary_text": {
              "type": "string"
            }
          }
        }
      },
      "invocation": {
        "cli": "transformers summarize --text <text> --file <file> --model <model> --max-length <max_length> --min-length <min_length> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "translate",
      "description": "Translate text between languages.",
      "inputs": {
        "text": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Text to translate."
        },
        "file": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Read text from this file."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "max_length": {
          "type": "integer",
          "nullable": true,
          "default": null,
          "description": "Maximum translation length."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "translation_text": {
              "type": "string"
            }
          }
        }
      },
      "invocation": {
        "cli": "transformers translate --text <text> --file <file> --model <model> --max-length <max_length> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "fill-mask",
      "description": "Predict the masked token in a sentence.",
      "inputs": {
        "text": {
          "type": "string",
          "description": "Text with a [MASK] token."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "top_k": {
          "type": "integer",
          "default": 5,
          "description": "Number of predictions to return."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "score": {
              "type": "number"
            },
            "token": {
              "type": "integer"
            },
            "token_str": {
              "type": "string"
            },
            "sequence": {
              "type": "string"
            }
          }
        }
      },
      "invocation": {
        "cli": "transformers fill-mask <text> --model <model> --top-k <top_k> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "image-classify",
      "description": "Classify an image.",
      "inputs": {
        "image": {
          "type": "string",
          "description": "Path or URL to the image."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "labels": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Comma-separated candidate labels for zero-shot classification."
        },
        "top_k": {
          "type": "integer",
          "default": 5,
          "description": "Number of top predictions to return."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "label": {
              "type": "string"
            },
            "score": {
              "type": "number"
            }
          }
        }
      },
      "invocation": {
        "cli": "transformers image-classify <image> --model <model> --labels <labels> --top-k <top_k> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "detect",
      "description": "Detect objects in an image.",
      "inputs": {
        "image": {
          "type": "string",
          "description": "Path or URL to the image."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "text": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Text query for open-vocabulary (grounded) detection."
        },
        "threshold": {
          "type": "number",
          "default": 0.5,
          "description": "Detection confidence threshold."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "label": {
              "type": "string"
            },
            "score": {
              "type": "number"
            },
            "box": {
              "type": "object",
              "properties": {
                "xmin": {
                  "type": "number"
                },
                "ymin": {
                  "type": "number"
                },
                "xmax": {
                  "type": "number"
                },
                "ymax": {
                  "type": "number"
                }
              }
            }
          }
        }
      },
      "invocation": {
        "cli": "transformers detect <image> --model <model> --text <text> --threshold <threshold> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "segment",
      "description": "Segment an image.",
      "inputs": {
        "image": {
          "type": "string",
          "description": "Path or URL to the image."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "points": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "JSON list of [x, y] points for SAM-style segmentation."
        },
        "point_labels": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "JSON list of point labels (1=foreground, 0=background) for SAM."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "object",
        "description": "Semantic segmentation class ratios or SAM mask metadata."
      },
      "invocation": {
        "cli": "transformers segment <image> --model <model> --points <points> --point-labels <point_labels> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "depth",
      "description": "Estimate a depth map from an image.",
      "inputs": {
        "image": {
          "type": "string",
          "description": "Path or URL to the image."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "output": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Path to save the depth map as a PNG image."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "size": {
            "type": "string",
            "description": "Depth map dimensions, e.g. '480x640'."
          },
          "output_path": {
            "type": "string",
            "description": "Path to saved PNG if --output was given."
          }
        }
      },
      "invocation": {
        "cli": "transformers depth <image> --model <model> --output <output> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "keypoints",
      "description": "Match keypoints between two images.",
      "inputs": {
        "images": {
          "type": "string",
          "description": "Paths to two images to match."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "object",
        "description": "Keypoint matching result (schema depends on pipeline output)."
      },
      "invocation": {
        "cli": "transformers keypoints <images> --model <model> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "video-classify",
      "description": "Classify a video.",
      "inputs": {
        "video": {
          "type": "string",
          "description": "Path to the video file."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "top_k": {
          "type": "integer",
          "default": 5,
          "description": "Number of top predictions to return."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "label": {
              "type": "string"
            },
            "score": {
              "type": "number"
            }
          }
        }
      },
      "invocation": {
        "cli": "transformers video-classify <video> --model <model> --top-k <top_k> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "transcribe",
      "description": "Transcribe speech from an audio file.",
      "inputs": {
        "audio": {
          "type": "string",
          "description": "Path or URL to the audio file."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "timestamps": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Enable timestamp prediction (e.g. 'true')."
        },
        "language": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Language code for transcription (e.g. 'en', 'fr')."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          }
        }
      },
      "invocation": {
        "cli": "transformers transcribe <audio> --model <model> --timestamps <timestamps> --language <language> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "audio-classify",
      "description": "Classify an audio file into categories.",
      "inputs": {
        "audio": {
          "type": "string",
          "description": "Path or URL to the audio file."
        },
        "labels": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Comma-separated candidate labels for zero-shot audio classification."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "top_k": {
          "type": "integer",
          "nullable": true,
          "default": null,
          "description": "Number of top predictions to return."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "label": {
              "type": "string"
            },
            "score": {
              "type": "number"
            }
          }
        }
      },
      "invocation": {
        "cli": "transformers audio-classify <audio> --labels <labels> --model <model> --top-k <top_k> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "speak",
      "description": "Synthesize speech from text and save to a WAV file.",
      "inputs": {
        "text": {
          "type": "string",
          "description": "Text to synthesize into speech."
        },
        "output": {
          "type": "string",
          "description": "Output WAV file path."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "output_path": {
            "type": "string"
          }
        }
      },
      "invocation": {
        "cli": "transformers speak <text> <output> --model <model> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "audio-generate",
      "description": "Generate audio (e.g. music) from a text description and save to a WAV file.",
      "inputs": {
        "text": {
          "type": "string",
          "description": "Text prompt describing the audio to generate."
        },
        "output": {
          "type": "string",
          "description": "Output WAV file path."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "output_path": {
            "type": "string"
          }
        }
      },
      "invocation": {
        "cli": "transformers audio-generate <text> <output> --model <model> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "vqa",
      "description": "Visual question answering using ``AutoModelForImageTextToText``.",
      "inputs": {
        "image": {
          "type": "string",
          "description": "Path or URL to the image."
        },
        "question": {
          "type": "string",
          "description": "Question about the image."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "max_new_tokens": {
          "type": "integer",
          "default": 256,
          "description": "Maximum tokens to generate."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "answer": {
            "type": "string"
          }
        }
      },
      "invocation": {
        "cli": "transformers vqa <image> <question> --model <model> --max-new-tokens <max_new_tokens> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "document-qa",
      "description": "Extractive document question answering using",
      "inputs": {
        "image": {
          "type": "string",
          "description": "Path or URL to the document image."
        },
        "question": {
          "type": "string",
          "description": "Question about the document."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "answer": {
            "type": "string"
          },
          "start": {
            "type": "integer"
          },
          "end": {
            "type": "integer"
          }
        }
      },
      "invocation": {
        "cli": "transformers document-qa <image> <question> --model <model> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "caption",
      "description": "Generate a caption for an image using ``AutoModelForImageTextToText``.",
      "inputs": {
        "image": {
          "type": "string",
          "description": "Path or URL to the image."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "max_new_tokens": {
          "type": "integer",
          "default": 64,
          "description": "Maximum tokens to generate."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "caption": {
            "type": "string"
          }
        }
      },
      "invocation": {
        "cli": "transformers caption <image> --model <model> --max-new-tokens <max_new_tokens> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "ocr",
      "description": "Extract text from an image using ``AutoModelForImageTextToText``.",
      "inputs": {
        "image": {
          "type": "string",
          "description": "Path or URL to the document image."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "max_new_tokens": {
          "type": "integer",
          "default": 512,
          "description": "Maximum tokens to generate."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          }
        }
      },
      "invocation": {
        "cli": "transformers ocr <image> --model <model> --max-new-tokens <max_new_tokens> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "multimodal-chat",
      "description": "Single-turn conversation with a model that accepts mixed inputs.",
      "inputs": {
        "prompt": {
          "type": "string",
          "description": "Text prompt for the conversation."
        },
        "model": {
          "type": "string",
          "description": "Model ID or local path."
        },
        "image": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Path or URL to an image."
        },
        "audio": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Path to an audio file."
        },
        "max_new_tokens": {
          "type": "integer",
          "default": 256,
          "description": "Maximum tokens to generate."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to run on (e.g. 'cpu', 'cuda', 'cuda:0', 'mps')."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype for model weights ('auto', 'float16', 'bfloat16', 'float32')."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code from the Hub."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token for gated/private models."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision (branch, tag, or commit SHA)."
        }
      },
      "outputs": {
        "type": "string",
        "description": "Generated text from multimodal conversation."
      },
      "invocation": {
        "cli": "transformers multimodal-chat <prompt> <model> --image <image> --audio <audio> --max-new-tokens <max_new_tokens> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "generate",
      "description": "Generate text from a prompt with full control over decoding.",
      "inputs": {
        "prompt": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Prompt text."
        },
        "file": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Read prompt from this file."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "assistant_model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Draft model for speculative/assisted decoding."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device (cpu, cuda, cuda:0, mps)."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype: auto, float16, bfloat16, float32."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision."
        },
        "max_new_tokens": {
          "type": "integer",
          "default": 256,
          "description": "Maximum new tokens to generate."
        },
        "temperature": {
          "type": "number",
          "nullable": true,
          "default": null,
          "description": "Sampling temperature."
        },
        "top_k": {
          "type": "integer",
          "nullable": true,
          "default": null,
          "description": "Top-k sampling."
        },
        "top_p": {
          "type": "number",
          "nullable": true,
          "default": null,
          "description": "Top-p (nucleus) sampling."
        },
        "num_beams": {
          "type": "integer",
          "nullable": true,
          "default": null,
          "description": "Number of beams for beam search."
        },
        "repetition_penalty": {
          "type": "number",
          "nullable": true,
          "default": null,
          "description": "Repetition penalty (1.0 = no penalty)."
        },
        "no_repeat_ngram_size": {
          "type": "integer",
          "nullable": true,
          "default": null,
          "description": "Prevent repeating n-grams of this size."
        },
        "do_sample": {
          "type": "boolean",
          "nullable": true,
          "default": null,
          "description": "Use sampling instead of greedy decoding."
        },
        "stream": {
          "type": "boolean",
          "default": false,
          "description": "Stream output token-by-token."
        },
        "watermark": {
          "type": "boolean",
          "default": false,
          "description": "Apply watermark to generated text."
        },
        "tools": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Path to a JSON file defining tools for function calling."
        },
        "grammar": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Constrain output format: 'json' for valid JSON output."
        },
        "quantization": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Load model quantized: 'bnb-4bit', 'bnb-8bit'."
        },
        "cache_quantization": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Quantize KV cache: '4bit', '8bit'."
        }
      },
      "outputs": {
        "type": "string",
        "description": "Generated text."
      },
      "invocation": {
        "cli": "transformers generate --prompt <prompt> --file <file> --model <model> --assistant-model <assistant_model> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision> --max-new-tokens <max_new_tokens> --temperature <temperature> --top-k <top_k> --top-p <top_p> --num-beams <num_beams> --repetition-penalty <repetition_penalty> --no-repeat-ngram-size <no_repeat_ngram_size> --do-sample <do_sample> --stream <stream> --watermark <watermark> --tools <tools> --grammar <grammar> --quantization <quantization> --cache-quantization <cache_quantization>"
      }
    },
    {
      "id": "detect-watermark",
      "description": "Detect whether text contains a watermark.",
      "inputs": {
        "text": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Text to check for watermark."
        },
        "file": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Read text from this file."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID (must match the model that generated the text)."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "prediction": {
            "type": "string"
          },
          "confidence": {
            "type": "number"
          }
        }
      },
      "invocation": {
        "cli": "transformers detect-watermark --text <text> --file <file> --model <model>"
      }
    },
    {
      "id": "embed",
      "description": "Compute embeddings for text or images.",
      "inputs": {
        "text": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Text to embed."
        },
        "file": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Read text from this file."
        },
        "image": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Path or URL to an image to embed."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "output": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Save embeddings to this file (.npy or .json)."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Dtype."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token."
        },
        "revision": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model revision."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "shape": {
            "type": "array",
            "items": {
              "type": "integer"
            }
          },
          "preview": {
            "type": "array",
            "items": {
              "type": "number"
            }
          }
        }
      },
      "invocation": {
        "cli": "transformers embed --text <text> --file <file> --image <image> --model <model> --output <output> --device <device> --dtype <dtype> --trust-remote-code <trust_remote_code> --token <token> --revision <revision>"
      }
    },
    {
      "id": "tokenize",
      "description": "Tokenize text and display the resulting tokens.",
      "inputs": {
        "text": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Text to tokenize."
        },
        "file": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Read text from this file."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code."
        },
        "show_ids": {
          "type": "boolean",
          "default": false,
          "description": "Show token IDs."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "tokens": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "token_ids": {
            "type": "array",
            "items": {
              "type": "integer"
            }
          },
          "num_tokens": {
            "type": "integer"
          }
        }
      },
      "invocation": {
        "cli": "transformers tokenize --text <text> --file <file> --model <model> --token <token> --trust-remote-code <trust_remote_code> --show-ids <show_ids>"
      }
    },
    {
      "id": "inspect",
      "description": "Inspect a model's configuration without downloading weights.",
      "inputs": {
        "model": {
          "type": "string",
          "description": "Model ID or local path to inspect."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code."
        }
      },
      "outputs": {
        "type": "object",
        "description": "Model configuration dictionary."
      },
      "invocation": {
        "cli": "transformers inspect <model> --token <token> --trust-remote-code <trust_remote_code>"
      }
    },
    {
      "id": "inspect-forward",
      "description": "Examine attention weights and hidden states from a forward pass.",
      "inputs": {
        "text": {
          "type": "string",
          "description": "Text to run through the model."
        },
        "model": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Model ID or local path."
        },
        "output": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Directory to save activations as .npy files."
        },
        "layers": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Comma-separated layer indices to inspect (default: all)."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code."
        }
      },
      "outputs": {
        "type": "object",
        "description": "Attention and hidden-state shape/stat summary per layer."
      },
      "invocation": {
        "cli": "transformers inspect-forward <text> --model <model> --output <output> --layers <layers> --token <token> --trust-remote-code <trust_remote_code>"
      }
    },
    {
      "id": "benchmark-quantization",
      "description": "Compare quality and performance across quantization methods.",
      "inputs": {
        "model": {
          "type": "string",
          "description": "Model ID or local path."
        },
        "methods": {
          "type": "string",
          "default": "bnb-4bit,bnb-8bit",
          "description": "Comma-separated quantization methods to compare: none, bnb-4bit, bnb-8bit."
        },
        "prompt": {
          "type": "string",
          "default": "The quick brown fox jumps over the lazy dog.",
          "description": "Prompt to use for benchmarking."
        },
        "max_new_tokens": {
          "type": "integer",
          "default": 50,
          "description": "Tokens to generate per run."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token."
        }
      },
      "outputs": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "method": {
              "type": "string"
            },
            "tokens_per_sec": {
              "type": "number"
            },
            "time_sec": {
              "type": "number"
            },
            "peak_memory_mb": {
              "type": "number"
            },
            "output_preview": {
              "type": "string"
            }
          }
        }
      },
      "invocation": {
        "cli": "transformers benchmark-quantization <model> --methods <methods> --prompt <prompt> --max-new-tokens <max_new_tokens> --trust-remote-code <trust_remote_code> --token <token>"
      }
    },
    {
      "id": "train",
      "description": "Fine-tune or pretrain a model on a dataset.",
      "inputs": {
        "task": {
          "type": "string",
          "description": "Task to train. One of: text-classification, token-classification, question-answering, summarization, translation, text-generation, language-modeling, image-classification, object-detection, semantic-segmentation, speech-recognition, audio-classification."
        },
        "model": {
          "type": "string",
          "description": "Model ID or local path."
        },
        "dataset": {
          "type": "string",
          "description": "Dataset name (Hub) or path (local file)."
        },
        "output": {
          "type": "string",
          "description": "Output directory for checkpoints and final model."
        },
        "subset": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Dataset subset/config name."
        },
        "epochs": {
          "type": "number",
          "default": 3.0,
          "description": "Number of training epochs."
        },
        "lr": {
          "type": "number",
          "default": 5e-05,
          "description": "Learning rate."
        },
        "batch_size": {
          "type": "integer",
          "default": 8,
          "description": "Per-device training batch size."
        },
        "eval_batch_size": {
          "type": "integer",
          "nullable": true,
          "default": null,
          "description": "Per-device eval batch size."
        },
        "max_seq_length": {
          "type": "integer",
          "default": 512,
          "description": "Maximum sequence length for tokenization."
        },
        "gradient_accumulation_steps": {
          "type": "integer",
          "default": 1,
          "description": "Gradient accumulation steps."
        },
        "warmup_ratio": {
          "type": "number",
          "default": 0.0,
          "description": "Warmup ratio."
        },
        "weight_decay": {
          "type": "number",
          "default": 0.0,
          "description": "Weight decay."
        },
        "eval_strategy": {
          "type": "string",
          "default": "epoch",
          "description": "Evaluation strategy: 'no', 'steps', 'epoch'."
        },
        "eval_steps": {
          "type": "integer",
          "nullable": true,
          "default": null,
          "description": "Evaluation interval (if strategy='steps')."
        },
        "save_strategy": {
          "type": "string",
          "default": "epoch",
          "description": "Save strategy: 'no', 'steps', 'epoch'."
        },
        "save_total_limit": {
          "type": "integer",
          "nullable": true,
          "default": null,
          "description": "Max checkpoints to keep."
        },
        "resume_from_checkpoint": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Path to checkpoint to resume from."
        },
        "load_best_model_at_end": {
          "type": "boolean",
          "default": true,
          "description": "Load best model after training."
        },
        "early_stopping": {
          "type": "boolean",
          "default": false,
          "description": "Enable early stopping."
        },
        "early_stopping_patience": {
          "type": "integer",
          "default": 3,
          "description": "Early stopping patience (eval rounds)."
        },
        "lora": {
          "type": "boolean",
          "default": false,
          "description": "Use LoRA for parameter-efficient fine-tuning."
        },
        "lora_r": {
          "type": "integer",
          "default": 16,
          "description": "LoRA rank."
        },
        "lora_alpha": {
          "type": "integer",
          "default": 32,
          "description": "LoRA alpha."
        },
        "lora_dropout": {
          "type": "number",
          "default": 0.05,
          "description": "LoRA dropout."
        },
        "quantization": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Quantize base model: 'bnb-4bit', 'bnb-8bit'."
        },
        "dtype": {
          "type": "string",
          "default": "auto",
          "description": "Training dtype: 'auto', 'float16', 'bfloat16', 'float32'."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device to train on: 'cpu', 'cuda', 'mps', 'tpu'."
        },
        "gradient_checkpointing": {
          "type": "boolean",
          "default": false,
          "description": "Enable gradient checkpointing."
        },
        "multi_gpu": {
          "type": "boolean",
          "default": false,
          "description": "Use all available GPUs on this machine."
        },
        "nnodes": {
          "type": "integer",
          "nullable": true,
          "default": null,
          "description": "Number of nodes for multi-node training (uses torchrun)."
        },
        "deepspeed": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "DeepSpeed config: 'zero2', 'zero3', or path to JSON."
        },
        "fsdp": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "FSDP strategy: 'full-shard', 'shard-grad-op', 'offload'."
        },
        "logging": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Logging integration: 'tensorboard', 'wandb', 'comet'."
        },
        "push_to_hub": {
          "type": "boolean",
          "default": false,
          "description": "Push final model to the Hub."
        },
        "hub_model_id": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Hub repository ID."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code."
        },
        "hpo": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HPO backend: 'optuna', 'ray'."
        },
        "hpo_trials": {
          "type": "integer",
          "default": 10,
          "description": "Number of HPO trials."
        },
        "from_scratch": {
          "type": "boolean",
          "default": false,
          "description": "Initialize model from scratch (random weights)."
        },
        "mlm": {
          "type": "boolean",
          "default": false,
          "description": "Use masked language modeling (for language-modeling task)."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "output_dir": {
            "type": "string"
          },
          "metrics": {
            "type": "object",
            "description": "Training metrics from the Trainer."
          }
        }
      },
      "invocation": {
        "cli": "transformers train <task> <model> <dataset> <output> --subset <subset> --epochs <epochs> --lr <lr> --batch-size <batch_size> --eval-batch-size <eval_batch_size> --max-seq-length <max_seq_length> --gradient-accumulation-steps <gradient_accumulation_steps> --warmup-ratio <warmup_ratio> --weight-decay <weight_decay> --eval-strategy <eval_strategy> --eval-steps <eval_steps> --save-strategy <save_strategy> --save-total-limit <save_total_limit> --resume-from-checkpoint <resume_from_checkpoint> --load-best-model-at-end <load_best_model_at_end> --early-stopping <early_stopping> --early-stopping-patience <early_stopping_patience> --lora <lora> --lora-r <lora_r> --lora-alpha <lora_alpha> --lora-dropout <lora_dropout> --quantization <quantization> --dtype <dtype> --device <device> --gradient-checkpointing <gradient_checkpointing> --multi-gpu <multi_gpu> --nnodes <nnodes> --deepspeed <deepspeed> --fsdp <fsdp> --logging <logging> --push-to-hub <push_to_hub> --hub-model-id <hub_model_id> --token <token> --trust-remote-code <trust_remote_code> --hpo <hpo> --hpo-trials <hpo_trials> --from-scratch <from_scratch> --mlm <mlm>"
      }
    },
    {
      "id": "quantize",
      "description": "Quantize a model and save it.",
      "inputs": {
        "model": {
          "type": "string",
          "description": "Model ID or local path to quantize."
        },
        "method": {
          "type": "string",
          "description": "Quantization method: bnb-4bit, bnb-8bit, gptq, awq."
        },
        "output": {
          "type": "string",
          "description": "Output directory for the quantized model."
        },
        "calibration_dataset": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Calibration dataset for GPTQ/AWQ (Hub name or local path)."
        },
        "calibration_samples": {
          "type": "integer",
          "default": 128,
          "description": "Number of calibration samples."
        },
        "bits": {
          "type": "integer",
          "default": 4,
          "description": "Target bit width (for GPTQ/AWQ)."
        },
        "group_size": {
          "type": "integer",
          "default": 128,
          "description": "Group size for GPTQ/AWQ."
        },
        "device": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Device for quantization."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token."
        },
        "push_to_hub": {
          "type": "boolean",
          "default": false,
          "description": "Push quantized model to Hub."
        },
        "hub_model_id": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Hub repo ID for push."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "output_path": {
            "type": "string"
          }
        }
      },
      "invocation": {
        "cli": "transformers quantize <model> <method> <output> --calibration-dataset <calibration_dataset> --calibration-samples <calibration_samples> --bits <bits> --group-size <group_size> --device <device> --trust-remote-code <trust_remote_code> --token <token> --push-to-hub <push_to_hub> --hub-model-id <hub_model_id>"
      }
    },
    {
      "id": "export",
      "description": "Export a model to a deployment-friendly format.",
      "inputs": {
        "fmt": {
          "type": "string",
          "description": "Export format: onnx, gguf, executorch."
        },
        "model": {
          "type": "string",
          "description": "Model ID or local path."
        },
        "output": {
          "type": "string",
          "description": "Output path (directory for ONNX, file for GGUF)."
        },
        "opset": {
          "type": "integer",
          "nullable": true,
          "default": null,
          "description": "ONNX opset version."
        },
        "task": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "Task for ONNX export (auto-detected if omitted)."
        },
        "trust_remote_code": {
          "type": "boolean",
          "default": false,
          "description": "Trust remote code."
        },
        "token": {
          "type": "string",
          "nullable": true,
          "default": null,
          "description": "HF Hub token."
        }
      },
      "outputs": {
        "type": "object",
        "properties": {
          "output_path": {
            "type": "string"
          },
          "format": {
            "type": "string"
          }
        }
      },
      "invocation": {
        "cli": "transformers export <fmt> <model> <output> --opset <opset> --task <task> --trust-remote-code <trust_remote_code> --token <token>"
      }
    }
  ]
}
