.. note::

   In Transformers v5, the `decoder` object returned by `AutoTokenizer` may
   differ from v4 due to internal tokenizer backend redesign. This does
   **not affect** the output of `tokenizer.decode(...)` and is intended behavior.
