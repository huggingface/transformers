name: transformers
dependencies:
    - python=3.8
    - pip
    - pip:
        - torch==1.10.0+cu111.moreh23.6.0
        - moreh-driver==23.6.0
        - -e ".[dev-torch]"

# conda env create -f hacenv.yml
# conda activate transformers
# update-moreh --force --target 23.6.0 --torch 1.10.0
