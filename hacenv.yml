name: transformers-dev
dependencies:
    - python=3.8
    - pip
    - pip:
        - torch==1.7.1+cu110.moreh23.4.1
        - moreh-driver==23.4.1
        - tensorflow==2.9.0
        - -e ".[dev]"

# conda env create -f hacenv.yml
# conda activate transformers-dev
# update-moreh --force --target 23.4.1 --tensorflow
